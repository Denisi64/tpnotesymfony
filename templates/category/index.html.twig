<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ForumInfinite - Category</title>

    {# Import Tailwind CSS #}
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    {# Import Font Awesome for Icons #}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">

<header>
    {% include 'parts/navbar.html.twig' %}
</header>

<main class="container mx-auto p-6">
    <h1 class="text-3xl font-semibold mb-6">Liste des Catégories</h1>

    <div class="mb-4">
        <a href="{{ path('app_category_new') }}" class="inline-block px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 shadow-md transition duration-300">
            Créer une nouvelle catégorie
        </a>
    </div>

    <div class="overflow-x-auto bg-white shadow-lg rounded-lg">
        <table class="min-w-full table-auto">
            <thead>
            <tr class="bg-gray-100">
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Id</th>
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Nom</th>
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Description</th>
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for category in categories %}
                <tr class="border-t hover:bg-gray-50">
                    <td class="px-4 py-2 text-sm text-gray-700">{{ category.id }}</td>
                    <td class="px-4 py-2 text-sm text-gray-700">{{ category.name }}</td>
                    <td class="px-4 py-2 text-sm text-gray-700">{{ category.description }}</td>
                    <td class="px-4 py-2 text-sm text-gray-700">
                        <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="inline-block text-yellow-600 hover:text-yellow-800 transition duration-200">
                            <i class="fas fa-edit mr-2"></i>Modifier
                        </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4" class="px-4 py-2 text-center text-gray-600">Aucun enregistrement trouvé</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</main>

<footer class="text-center text-gray-500 p-4">
    &copy; {{ "now"|date("Y") }} ForumInfinite
</footer>

{# Import le JavaScript #}
<script src="{{ asset('build/runtime.js') }}" defer></script>
<script src="{{ asset('build/app.js') }}" defer></script>

</body>
</html>
