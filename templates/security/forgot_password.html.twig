{% include 'parts/navbar.html.twig' %}
<!DOCTYPE html>
<html lang="fr">
<body class="font-montserrat text-sm bg-gray-100 dark:bg-gray-900">
<div class="flex items-center justify-center min-h-screen">
    <form class="w-full max-w-md bg-white rounded-lg shadow-lg p-6 dark:bg-gray-800" action="{{ path('page_forgot_password') }}" method="POST">
        <h1 class="text-2xl font-bold text-center mb-6 text-gray-800 dark:text-gray-200">Mot de passe perdu ?</h1>
        <p class="text-center text-sm font-medium opacity-70 mb-6">Vous recevrez un email pour réinitialiser votre mot de passe</p>

        {% for message in app.flashes('error') %}
            <div class="mb-4 p-4 text-sm text-red-600 bg-red-100 rounded dark:bg-red-700 dark:text-red-100">
                {{ message }}
            </div>
        {% endfor %}

        <div class="mb-4">
            <label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Adresse E-mail</label>
            <input type="email" id="email" name="email" class="mt-1 block w-full px-4 py-2 text-gray-900 bg-gray-50 border border-gray-300 rounded-lg dark:bg-gray-700 dark:text-white dark:border-gray-600 focus:ring-blue-500 focus:border-blue-500" placeholder="Votre adresse e-mail" required />
        </div>

        <div class="mb-6">
            <button type="submit" class="w-full px-4 py-2 text-white bg-red-600 rounded-lg hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500 dark:bg-red-500 dark:hover:bg-red-600">
                Réinitialiser le mot de passe
            </button>
        </div>

        <div class="mt-4 text-center">
            <a href="{{ path('app_login') }}" class="text-blue-500 hover:underline dark:text-blue-400">Retour à la connexion</a>
        </div>
    </form>
</div>
</body>
</html>
