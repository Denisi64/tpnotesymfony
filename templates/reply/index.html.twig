<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ForumInfinite - Replies{% endblock %}</title>

    {# Import Tailwind CSS #}
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    {# Import Font Awesome for Icons #}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">

<header>
    {% include 'parts/navbar.html.twig' %}
</header>

<main class="container mx-auto p-6">
    <h1 class="text-3xl font-semibold mb-6">Liste des Replies</h1>

    <div class="mb-4">
        <a href="{{ path('app_reply_new') }}" class="inline-block px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 shadow-md transition duration-300">
            Créer une nouvelle Réponse
        </a>
    </div>

    <div class="overflow-x-auto bg-white shadow-lg rounded-lg">
        <table class="min-w-full table-auto">
            <thead>
            <tr class="bg-gray-100">
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Id</th>
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Content</th>
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Created At</th>
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Updated At</th>
                <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for reply in replies %}
                <tr class="border-t hover:bg-gray-50">
                    <td class="px-4 py-2 text-sm text-gray-700">{{ reply.id }}</td>
                    <td class="px-4 py-2 text-sm text-gray-700">{{ reply.content }}</td>
                    <td class="px-4 py-2 text-sm text-gray-700">{{ reply.createdAt ? reply.createdAt|date('Y-m-d') : '' }}</td>
                    <td class="px-4 py-2 text-sm text-gray-700">{{ reply.updatedAt ? reply.updatedAt|date('Y-m-d') : '' }}</td>
                    <td class="px-4 py-2 text-sm text-gray-700">
                        <a href="{{ path('app_reply_edit', {'id': reply.id}) }}" class="inline-block text-yellow-600 hover:text-yellow-800 transition duration-200">
                            <i class="fas fa-edit mr-2"></i>Edit
                        </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="px-4 py-2 text-center text-gray-600">Aucune réponse trouvée</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</main>

<footer class="text-center text-gray-500 p-4">
    &copy; {{ "now"|date("Y") }} ForumInfinite
</footer>

{# Import le JavaScript #}
<script src="{{ asset('build/runtime.js') }}" defer></script>
<script src="{{ asset('build/app.js') }}" defer></script>

</body>
</html>
