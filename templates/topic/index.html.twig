<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ForumInfinite - Topic{% endblock %}</title>

    {# Import Tailwind CSS #}
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    {# Import Font Awesome for Icons #}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">

<header>
    {% include 'parts/navbar.html.twig' %}
</header>

{% block body %}
    <div class="container mx-auto p-6">
        <h1 class="text-3xl font-semibold mb-6">Liste des Topics</h1>

        <div class="mb-4">
            <a href="{{ path('app_topic_new') }}" class="inline-block px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 shadow-md transition duration-300">
                Créer un nouveau Topic
            </a>
        </div>

        <div class="overflow-x-auto bg-white shadow-lg rounded-lg">
            <table class="min-w-full table-auto">
                <thead>
                <tr class="bg-gray-100">
                    <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Id</th>
                    <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Title</th>
                    <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Content</th>
                    <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Created At</th>
                    <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Updated At</th>
                    <th class="px-4 py-2 text-left text-sm font-medium text-gray-600">Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for topic in topics %}
                    <tr class="border-t hover:bg-gray-50">
                        <td class="px-4 py-2 text-sm text-gray-700">{{ topic.id }}</td>
                        <td class="px-4 py-2 text-sm text-gray-700">{{ topic.title }}</td>
                        <td class="px-4 py-2 text-sm text-gray-700">{{ topic.content }}</td>
                        <td class="px-4 py-2 text-sm text-gray-700">{{ topic.createdAt ? topic.createdAt|date('Y-m-d') : '' }}</td>
                        <td class="px-4 py-2 text-sm text-gray-700">{{ topic.updatedAt ? topic.updatedAt|date('Y-m-d') : '' }}</td>
                        <td class="px-4 py-2 text-sm text-gray-700">
                            <a href="{{ path('app_topic_show', {'id': topic.id}) }}" class="inline-block text-blue-600 hover:text-blue-800 transition duration-200">
                                <i class="fas fa-eye mr-2"></i>Show
                            </a>
                            |
                            <a href="{{ path('app_topic_edit', {'id': topic.id}) }}" class="inline-block text-yellow-600 hover:text-yellow-800 transition duration-200">
                                <i class="fas fa-edit mr-2"></i>Edit
                            </a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="px-4 py-2 text-center text-gray-600">No records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

</body>
</html>
